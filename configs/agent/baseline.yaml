# Baseline agent run: no overrides needed, these match DQNConfig's defaults
# (src/relearn/config.py). Spelled out here as a reference for every field
# available to override.
batch_size: 128
gamma: 0.99
eps_start: 0.9
eps_end: 0.01
eps_decay: 2500
tau: 0.005
lr: 3.0e-4
seed: 42
num_episodes: 300
replay_capacity: 10000
